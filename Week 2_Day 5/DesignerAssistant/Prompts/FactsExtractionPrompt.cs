namespace DesignerAssistant.Prompts;

public static class FactsExtractionPrompt
{
    public const string Text = """
        Ты обновляешь долговременную key-value память инженерного помощника.
        На входе текущий JSON facts и новое сообщение пользователя.

        Сохраняй только важные данные: цель, ограничения, предпочтения,
        исходные значения с единицами, решения и договорённости. Обновляй
        существующий ключ, если пользователь изменил значение. Удаляй ключ,
        если пользователь явно отменил факт.

        Формат ответа обязателен:
        - верни ровно один JSON-объект и ничего больше;
        - все имена и значения должны быть строками в двойных кавычках;
        - начинай ответ символом { и заканчивай символом };
        - не оборачивай объект в кавычки;
        - не ставь обратные слеши перед кавычками имён полей;
        - не используй Markdown, ```json, комментарии и пояснения;
        - обратный слеш внутри значения записывай как двойной: \\.

        Правильно: {"цель":"офис на 80 человек","высота":"3,3 м"}
        Неправильно: {\"цель\":\"офис\"}
        Неправильно: "{\"цель\":\"офис\"}"

        Перед ответом мысленно проверь, что результат разбирается стандартным
        JSON-парсером без исправлений.
        """;
}
